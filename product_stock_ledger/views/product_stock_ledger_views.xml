<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ══════════════════════════════════════════
         SEARCH VIEW
    ══════════════════════════════════════════ -->
    <record id="view_product_stock_ledger_search" model="ir.ui.view">
        <field name="name">product.stock.ledger.search</field>
        <field name="model">product.stock.ledger</field>
        <field name="arch" type="xml">
            <search string="Product Stock Ledger">

                <!-- Quick-search fields -->
                <field name="product_id"   string="Product"/>
                <field name="warehouse_id" string="Warehouse"/>
                <field name="voucher"      string="Voucher"/>
                <field name="particulars"  string="Particulars"/>

                <!-- Filters -->
                <filter string="Received"      name="filter_received"
                        domain="[('rec_qty','>',0)]"/>
                <filter string="Issued"        name="filter_issued"
                        domain="[('issue_qty','>',0)]"/>
                <filter string="Internal Move" name="filter_internal"
                        domain="[('move_type','=','INT')]"/>
                <separator/>
                <filter string="Today"         name="filter_today"
                        domain="[('date','&gt;=',datetime.datetime.combine(context_today(),datetime.time(0,0,0))),
                                 ('date','&lt;=',datetime.datetime.combine(context_today(),datetime.time(23,59,59)))]"/>
                <filter string="This Month"    name="filter_month"
                        domain="[('date','&gt;=', (context_today() + relativedelta(day=1)).strftime('%Y-%m-%d')),
                                 ('date','&lt;=', (context_today() + relativedelta(day=31)).strftime('%Y-%m-%d'))]"/>

                <!-- Group By -->
                <group expand="0" string="Group By">
                    <filter string="Product"   name="group_product"
                            context="{'group_by':'product_id'}"/>
                    <filter string="Warehouse" name="group_warehouse"
                            context="{'group_by':'warehouse_id'}"/>
                    <filter string="Type"      name="group_type"
                            context="{'group_by':'move_type'}"/>
                    <filter string="Date"      name="group_date"
                            context="{'group_by':'date:month'}"/>
                </group>

            </search>
        </field>
    </record>

    <!-- ══════════════════════════════════════════
         LIST (TREE) VIEW
    ══════════════════════════════════════════ -->
    <record id="view_product_stock_ledger_list" model="ir.ui.view">
        <field name="name">product.stock.ledger.list</field>
        <field name="model">product.stock.ledger</field>
        <field name="arch" type="xml">
            <list string="Product Stock Ledger"
                  create="false" edit="false" delete="false"
                  decoration-success="move_type == 'IN'"
                  decoration-danger="move_type == 'OUT'"
                  decoration-muted="move_type == 'INT'">

                <field name="product_id"     string="Product"        width="180px" optional="show"/>
                <field name="warehouse_id"   string="Warehouse"      width="120px" optional="show"/>
                <field name="date"           string="Date"           widget="date" width="110px" optional="show"/>
                <field name="voucher"        string="Voucher"        width="130px" optional="show"/>
                <field name="particulars"    string="Particulars"    width="260px" optional="show"/>
                <field name="move_type"      string="Type"           width="90px"  optional="show"/>
                <field name="rec_qty"        string="Rec. Qty"       sum="Total Received"
                                             width="90px"  optional="show"/>
                <field name="rec_rate"       string="Rec. Rate"      width="90px"  optional="show"/>
                <field name="issue_qty"      string="Issue Qty"      sum="Total Issued"
                                             width="90px"  optional="show"/>
                <field name="issue_rate"     string="Issue Rate"     width="90px"  optional="show"/>
                <field name="balance"        string="Balance"        width="90px"  optional="show"/>
                <field name="uom"            string="Unit"           width="60px"  optional="show"/>
                <field name="invoice_status" string="Invoice Status" width="110px" optional="show"/>

            </list>
        </field>
    </record>

    <!-- ══════════════════════════════════════════
         WINDOW ACTION
    ══════════════════════════════════════════ -->
    <record id="action_product_stock_ledger_window" model="ir.actions.act_window">
        <field name="name">Product Stock Ledger</field>
        <field name="res_model">product.stock.ledger</field>
        <field name="view_mode">list</field>
        <field name="view_id" ref="view_product_stock_ledger_list"/>
        <field name="search_view_id" ref="view_product_stock_ledger_search"/>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No stock movements found.
            </p>
            <p>
                This ledger shows all confirmed stock moves with their
                received quantity, issued quantity, and running balance.
            </p>
        </field>
    </record>

</odoo>
