<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Add From/To Date Filter to Delivery Notes Search View -->
    <record id="view_picking_internal_search_date_range" model="ir.ui.view">
        <field name="name">stock.picking.search.date.range</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_internal_search"/>
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='ready']" position="after">
                <separator/>
                <filter string="From Date" name="filter_from_date"
                        date="scheduled_date"/>
                <filter string="To Date" name="filter_to_date"
                        date="scheduled_date"/>
            </xpath>
        </field>
    </record>
</odoo>