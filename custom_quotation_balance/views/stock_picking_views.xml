<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_picking_form_partner_balance" model="ir.ui.view">
        <field name="name">stock.picking.form.partner.balance</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">

            <!-- Make currency available -->
            <field name="partner_id" position="before">
                <field name="currency_id" invisible="1"/>
            </field>

            <!-- Add super compact balance display in one row -->
            <field name="partner_id" position="after">

                <label for="partner_total_invoiced"
                       string="Balance Summary"
                       invisible="not partner_id"
                       class="fw-bold"/>
                <div class="o_row" invisible="not partner_id">
                    <span>Invoiced:</span>
                    <field name="partner_total_invoiced"
                           widget="monetary"
                           readonly="1"
                           nolabel="1"
                           class="fw-bold"/>

                    <span style="margin-left: 15px;">Paid:</span>
                    <field name="partner_total_paid"
                           widget="monetary"
                           readonly="1"
                           nolabel="1"
                           decoration-success="partner_total_paid > 0"
                           class="fw-bold"/>

                    <span style="margin-left: 15px;">Due:</span>
                    <field name="partner_balance_due"
                           widget="monetary"
                           readonly="1"
                           nolabel="1"
                           decoration-danger="partner_balance_due > 0"
                           decoration-success="partner_balance_due == 0"
                           class="fw-bold"/>
                </div>

            </field>

        </field>
    </record>
</odoo>