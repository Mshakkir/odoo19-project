<!-- Copyright 2017 ForgeFlow S.L.
     License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl). -->
<odoo>
    <!-- Sale Order Form View -->
    <record id="view_order_form" model="ir.ui.view">
        <field name="name">sale.order.form - fixed_discount</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form" />
        <field name="arch" type="xml">
            <!-- Add to list view (tree) -->
            <xpath
                expr="//field[@name='order_line']/tree/field[@name='discount']"
                position="before"
            >
                <field
                    name="discount_fixed"
                    groups="account_invoice_fixed_discount.group_fixed_discount"
                    optional="show"
                />
            </xpath>
            <!-- Add to form view -->
            <xpath
                expr="//field[@name='order_line']/form//field[@name='discount']"
                position="before"
            >
                <field
                    name="discount_fixed"
                    groups="account_invoice_fixed_discount.group_fixed_discount"
                />
            </xpath>
        </field>
    </record>

    <!-- Quotation Template (optional - if you use quotation templates) -->
    <record id="view_sale_quote_template_form" model="ir.ui.view">
        <field name="name">sale.order.template.form - fixed_discount</field>
        <field name="model">sale.order.template</field>
        <field name="inherit_id" ref="sale_management.sale_order_template_view_form" />
        <field name="arch" type="xml">
            <xpath
                expr="//field[@name='sale_order_template_line_ids']/tree/field[@name='discount']"
                position="before"
            >
                <field
                    name="discount_fixed"
                    groups="account_invoice_fixed_discount.group_fixed_discount"
                    optional="show"
                />
            </xpath>
        </field>
    </record>
</odoo>