<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Add From/To Date Filter to Delivery Notes Search View -->
    <record id="view_picking_internal_search_date_range" model="ir.ui.view">
        <field name="name">stock.picking.search.date.range</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_internal_search"/>
        <field name="arch" type="xml">
            <!-- Add date range filters -->
            <xpath expr="//search" position="inside">
                <separator/>
                <filter string="From Date" name="filter_from_date"
                        date="scheduled_date"/>
                <filter string="To Date" name="filter_to_date"
                        date="scheduled_date"/>
                <separator/>
                <filter string="Date Range" name="date_range"
                        domain="[]"
                        context="{'search_default_date_range': 1}"/>
            </xpath>
        </field>
    </record>

    <!-- Add grouping by date -->
    <record id="view_picking_search_inherit_date" model="ir.ui.view">
        <field name="name">stock.picking.search.date.inherit</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_internal_search"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='group_by']" position="inside">
                <filter string="Scheduled Date (Day)" name="group_scheduled_date_day"
                        context="{'group_by': 'scheduled_date:day'}"/>
                <filter string="Scheduled Date (Week)" name="group_scheduled_date_week"
                        context="{'group_by': 'scheduled_date:week'}"/>
                <filter string="Scheduled Date (Month)" name="group_scheduled_date_month"
                        context="{'group_by': 'scheduled_date:month'}"/>
            </xpath>
        </field>
    </record>

    <!-- Enhanced delivery tree view -->
    <record id="view_picking_tree_date_columns" model="ir.ui.view">
        <field name="name">stock.picking.tree.date.columns</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.vpicktree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='scheduled_date']" position="attributes">
                <attribute name="widget">date</attribute>
                <attribute name="optional">show</attribute>
            </xpath>
        </field>
    </record>
</odoo>