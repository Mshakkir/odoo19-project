<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Extend the existing Accounting Report wizard -->
    <record id="accounting_report_extend_view_form" model="ir.ui.view">
        <field name="name">accounting.report.extend.form</field>
        <field name="model">accounting.report</field>
        <field name="inherit_id" ref="accounting_pdf_reports.accounting_report_view"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='account_report_id']" position="after">
                <field name="analytic_account_ids" widget="many2many_tags"/>
                <field name="analytic_filter_mode"
                       invisible="not analytic_account_ids"
                       widget="radio"/>
            </xpath>
        </field>
    </record>
</odoo>