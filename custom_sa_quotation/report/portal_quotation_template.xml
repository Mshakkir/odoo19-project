<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ========================================================= -->
    <!-- Hide portal header & footer only for Sale Orders -->
    <!-- ========================================================= -->
    <template id="portal_layout_hide_header_footer"
              inherit_id="portal.portal_layout"
              priority="100">

        <!-- Hide portal navbar -->
        <xpath expr="//header[contains(@class,'o_portal_navbar')]"
               position="attributes">
            <attribute name="t-att-style">
                display:none !important; if '/my/orders/' in request.httprequest.path else None
            </attribute>
        </xpath>

        <!-- Hide footer -->
        <xpath expr="//footer[@id='footer']"
               position="attributes">
            <attribute name="t-att-style">
                display:none !important; if '/my/orders/' in request.httprequest.path else None
            </attribute>
        </xpath>

        <!-- Remove page padding -->
        <xpath expr="//div[@id='wrapwrap']" position="attributes">
            <attribute name="t-att-style">
                padding:0;margin:0; if '/my/orders/' in request.httprequest.path else None
            </attribute>
        </xpath>

    </template>

    <!-- ========================================================= -->
    <!-- Custom Sale Order Portal Content -->
    <!-- ========================================================= -->
    <template id="custom_portal_sale_order"
              inherit_id="sale.sale_order_portal_content"
              priority="100">

        <xpath expr="//div[@id='introduction']" position="attributes">
            <attribute name="style">display:none !important;</attribute>
        </xpath>

        <xpath expr="//div[@id='informations']" position="attributes">
            <attribute name="style">display:none !important;</attribute>
        </xpath>

        <xpath expr="//div[@name='sol_table']" position="attributes">
            <attribute name="style">display:none !important;</attribute>
        </xpath>

        <xpath expr="//div[@id='total']" position="attributes">
            <attribute name="style">display:none !important;</attribute>
        </xpath>

        <xpath expr="//section[@id='signature']" position="attributes">
            <attribute name="style">display:none !important;</attribute>
        </xpath>

        <xpath expr="//section[@id='terms']" position="attributes">
            <attribute name="style">display:none !important;</attribute>
        </xpath>

        <!-- Custom CSS -->
        <xpath expr="//div[@id='content']" position="before">
            <style>
                #wrap {
                    padding-top: 0 !important;
                    margin-top: 0 !important;
                }
                .custom-quotation-wrapper {
                    width: 100%;
                    max-width: 100%;
                    padding: 20px;
                }
            </style>
        </xpath>

        <!-- Inject custom quotation -->
        <xpath expr="//div[@id='content']" position="before">
            <div class="custom-quotation-wrapper">
                <t t-set="o" t-value="sale_order"/>
                <t t-set="docs" t-value="[sale_order]"/>
                <t t-call="custom_sa_quotation.custom_saleorder_report"/>
            </div>
        </xpath>

    </template>

</odoo>
