<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Security Group: Warehouse Manager -->
    <record id="group_warehouse_manager" model="res.groups">
        <field name="name">Warehouse Manager</field>
        <field name="category_id" ref="base.module_category_inventory_inventory"/>
        <field name="implied_ids" eval="[(4, ref('stock.group_stock_manager'))]"/>
    </record>

    <!-- Security Group: Warehouse User -->
    <record id="group_warehouse_user" model="res.groups">
        <field name="name">Warehouse User</field>
        <field name="category_id" ref="base.module_category_inventory_inventory"/>
        <field name="implied_ids" eval="[(4, ref('stock.group_stock_user'))]"/>
    </record>

    <!-- Record Rule: Stock Picking - User can see their warehouse deliveries -->
    <record id="stock_picking_warehouse_user_rule" model="ir.rule">
        <field name="name">Stock Picking: User Warehouse Access</field>
        <field name="model_id" ref="stock.model_stock_picking"/>
        <field name="groups" eval="[(4, ref('group_warehouse_user'))]"/>
        <field name="domain_force">[
            '|',
            ('warehouse_id', '=', False),
            ('warehouse_id', 'in', user.warehouse_ids.ids)
        ]</field>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- Record Rule: Stock Picking - Manager can see all -->
    <record id="stock_picking_warehouse_manager_rule" model="ir.rule">
        <field name="name">Stock Picking: Manager Full Access</field>
        <field name="model_id" ref="stock.model_stock_picking"/>
        <field name="groups" eval="[(4, ref('group_warehouse_manager'))]"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

    <!-- Record Rule: Users can only see themselves (for dashboard) -->
    <record id="res_users_warehouse_rule" model="ir.rule">
        <field name="name">Users: Own Record Access</field>
        <field name="model_id" ref="base.model_res_users"/>
        <field name="groups" eval="[(4, ref('group_warehouse_user'))]"/>
        <field name="domain_force">[('id', '=', user.id)]</field>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

</odoo>