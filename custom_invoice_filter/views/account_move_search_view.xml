<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit the invoice tree view -->
    <record id="view_invoice_tree_add_filter_button" model="ir.ui.view">
        <field name="name">account.move.tree.filter.button</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_invoice_tree"/>
        <field name="arch" type="xml">
            <!-- Add button directly in tree view -->
            <tree position="attributes">
                <attribute name="create">true</attribute>
            </tree>
            <tree position="before">
                <button name="%(custom_invoice_filter.action_invoice_date_filter_wizard)d"
                        string="📅 Filter by Date"
                        type="action"
                        class="oe_highlight"
                        icon="fa-calendar"/>
            </tree>
        </field>
    </record>
</odoo>