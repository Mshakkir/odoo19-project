<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Client Action for Date Filter View -->
    <record id="action_sale_order_date_filter" model="ir.actions.client">
        <field name="name">Sale Orders with Date Filter</field>
        <field name="tag">sale_date_filter_view</field>
        <field name="target">current</field>
    </record>

    <!-- Menu Item -->
    <menuitem
        id="menu_sale_order_date_filter"
        name="Orders (Date Filter)"
        parent="sale.sale_menu_root"
        action="action_sale_order_date_filter"
        sequence="5"
    />
</odoo>