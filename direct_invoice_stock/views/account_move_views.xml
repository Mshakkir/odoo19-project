<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Invoice Form View Inherit -->
    <record id="view_move_form_inherit_direct_invoice" model="ir.ui.view">
        <field name="name">account.move.form.inherit.direct.invoice</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">

            <!-- Add smart button for delivery order -->
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_delivery"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-truck"
                        invisible="not picking_id"
                        groups="stock.group_stock_user">
                    <field name="picking_id" invisible="1"/>
                    <div class="o_field_widget o_stat_info">
                        <span class="o_stat_text">Delivery</span>
                    </div>
                </button>
            </xpath>

            <!-- Add checkbox in header area for better visibility -->
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="create_delivery"
                       invisible="move_type != 'out_invoice'"
                       widget="boolean_toggle"
                       string="🚚 Auto Create Delivery Order"/>
            </xpath>

        </field>
    </record>

    <!-- Add field in tree view for visibility -->
    <record id="view_invoice_tree_inherit_direct_invoice" model="ir.ui.view">
        <field name="name">account.move.tree.inherit.direct.invoice</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_invoice_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='state']" position="before">
                <field name="picking_id" optional="show" widget="badge"/>
            </xpath>
        </field>
    </record>

</odoo>