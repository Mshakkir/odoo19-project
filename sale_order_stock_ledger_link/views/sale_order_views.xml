<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit Sale Order Form View to add product list link -->
    <record id="view_order_form_product_list_link" model="ir.ui.view">
        <field name="name">sale.order.form.product.list.link</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">

            <!-- Add button in list view after product_template_id -->
            <xpath expr="//field[@name='order_line']/list/field[@name='product_template_id']" position="after">
                <button name="action_view_product_list"
                        type="object"
                        icon="fa-list-alt"
                        title="View Product Details"
                        string=""
                        width="40px"
                        class="btn-link p-0"
                        invisible="not product_id"/>
            </xpath>

            <!-- Add button in form view after product field -->
            <xpath expr="//field[@name='order_line']/form//field[@name='product_id']" position="after">
                <button name="action_view_product_list"
                        type="object"
                        string="📋 View Product"
                        class="btn-link"
                        invisible="not product_id"/>
            </xpath>

            <!-- Add button in kanban view -->
            <xpath expr="//field[@name='order_line']/kanban//field[@name='product_id']" position="after">
                <button name="action_view_product_list"
                        type="object"
                        string="View Product"
                        class="btn-sm btn-secondary mt-1"
                        invisible="not product_id"/>
            </xpath>

        </field>
    </record>

    <!-- Inherit Invoice/Bill Form View to add product list link -->
    <record id="view_move_form_product_list_link" model="ir.ui.view">
        <field name="name">account.move.form.product.list.link</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">

            <!-- Add button in invoice line list view after product -->
            <xpath expr="//field[@name='invoice_line_ids']/list/field[@name='product_id']" position="after">
                <button name="action_view_product_list"
                        type="object"
                        icon="fa-list-alt"
                        title="View Product Details"
                        string=""
                        width="40px"
                        class="btn-link p-0"
                        invisible="not product_id or display_type in ('line_section', 'line_subsection', 'line_note')"/>
            </xpath>

            <!-- Add button in invoice line form view after product field -->
            <xpath expr="//field[@name='invoice_line_ids']/form//field[@name='product_id']" position="after">
                <button name="action_view_product_list"
                        type="object"
                        string="📋 View Product"
                        class="btn-link"
                        invisible="not product_id"/>
            </xpath>

            <!-- Add button in invoice line kanban view -->
            <xpath expr="//field[@name='invoice_line_ids']/kanban//field[@name='product_id']" position="after">
                <button name="action_view_product_list"
                        type="object"
                        string="View Product"
                        class="btn-sm btn-secondary mt-1"
                        invisible="not product_id"/>
            </xpath>

        </field>
    </record>

</odoo>

