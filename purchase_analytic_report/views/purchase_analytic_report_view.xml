<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Report List/Tree View -->
    <record id="view_purchase_analytic_report_list" model="ir.ui.view">
        <field name="name">purchase.analytic.report.list</field>
        <field name="model">purchase.analytic.report</field>
        <field name="arch" type="xml">
            <list string="Purchase Analytic Report" create="false" edit="false" delete="false">
                <field name="invoice_date"/>
                <field name="invoice_number"/>
                <field name="vendor_id"/>
                <field name="warehouse_id"/>
                <field name="product_id"/>
                <field name="quantity" sum="Total Quantity"/>
                <field name="uom_id"/>
                <field name="unit_price"/>
                <field name="net_amount" sum="Total Amount"/>
                <field name="analytic_account_id" optional="hide"/>
            </list>
        </field>
    </record>

    <!-- Report Search View -->
    <record id="view_purchase_analytic_report_search" model="ir.ui.view">
        <field name="name">purchase.analytic.report.search</field>
        <field name="model">purchase.analytic.report</field>
        <field name="arch" type="xml">
            <search string="Purchase Analytic Report">
                <field name="invoice_number"/>
                <field name="vendor_id"/>
                <field name="product_id"/>
                <field name="warehouse_id"/>
                <field name="analytic_account_id"/>
                <group expand="0" string="Group By">
                    <filter string="Vendor" name="group_vendor"
                            context="{'group_by': 'vendor_id'}"/>
                    <filter string="Product" name="group_product"
                            context="{'group_by': 'product_id'}"/>
                    <filter string="Warehouse" name="group_warehouse"
                            context="{'group_by': 'warehouse_id'}"/>
                    <filter string="Analytic Account" name="group_analytic"
                            context="{'group_by': 'analytic_account_id'}"/>
                    <filter string="Date" name="group_date"
                            context="{'group_by': 'invoice_date'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Report Action -->
    <record id="action_purchase_analytic_report" model="ir.actions.act_window">
        <field name="name">Purchase Analytic Report</field>
        <field name="res_model">purchase.analytic.report</field>
        <field name="view_mode">list</field>
        <field name="search_view_id" ref="view_purchase_analytic_report_search"/>
    </record>
</odoo>