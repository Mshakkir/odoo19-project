<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit Sale Order Form View to make product clickable -->
    <record id="view_order_form_product_ledger_link" model="ir.ui.view">
        <field name="name">sale.order.form.product.ledger.link</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">

            <!-- Modify the list view of order lines -->
            <xpath expr="//field[@name='order_line']/list/field[@name='product_template_id']" position="attributes">
                <attribute name="widget">sol_product_many2one</attribute>
                <attribute name="options">{'show_label_warning': True, 'open_action': 'action_view_product_stock_ledger'}</attribute>
            </xpath>

            <!-- Also add icon button next to product for direct access -->
            <xpath expr="//field[@name='order_line']/list/field[@name='product_template_id']" position="after">
                <button name="action_view_product_stock_ledger"
                        type="object"
                        icon="fa-line-chart"
                        title="View Stock Ledger"
                        class="btn-link"
                        invisible="not product_id"/>
            </xpath>

            <!-- Also modify form view inside order lines -->
            <xpath expr="//field[@name='order_line']/form//field[@name='product_id']" position="after">
                <button name="action_view_product_stock_ledger"
                        type="object"
                        string="View Stock Ledger"
                        icon="fa-line-chart"
                        class="btn-link"
                        invisible="not product_id"/>
            </xpath>

        </field>
    </record>

</odoo>