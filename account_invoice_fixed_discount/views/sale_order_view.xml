<odoo>
    <record id="view_order_form_global_discount" model="ir.ui.view">
        <field name="name">sale.order.form.global.discount</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">

            <!-- Insert Global Discount Field Under Order Lines -->
            <xpath expr="//sheet//div[contains(@class,'o_notebook')]" position="before">
                <group>
                    <field name="global_discount_fixed"/>
                </group>
            </xpath>

        </field>
    </record>
</odoo>
