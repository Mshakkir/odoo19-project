<!-- Copyright 2017 ForgeFlow S.L.
     License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl). -->
<odoo>
    <!-- Add discount_fixed field to invoice lines -->
    <record id="view_move_form" model="ir.ui.view">
        <field name="name">account.move.form - fixed_discount</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <xpath
                expr="//field[@name='invoice_line_ids']/list/field[@name='discount']"
                position="before"
            >
                <field
                    name="discount_fixed"
                    groups="account_invoice_fixed_discount.group_fixed_discount"
                    optional="show"
                />
            </xpath>
            <xpath
                expr="//field[@name='invoice_line_ids']/form/sheet/group/field[@name='discount']"
                position="before"
            >
                <field
                    name="discount_fixed"
                    groups="account_invoice_fixed_discount.group_fixed_discount"
                />
            </xpath>
        </field>
    </record>

    <!-- Add discount summary fields in invoice totals section -->
    <record id="view_move_form_totals" model="ir.ui.view">
        <field name="name">account.move.form - discount_totals</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <!-- Add before "Untaxed Amount" -->
            <xpath expr="//field[@name='amount_untaxed']" position="before">
                <field name="amount_before_discount"
                       widget="monetary"
                       options="{'currency_field': 'currency_id'}"
                       groups="account_invoice_fixed_discount.group_fixed_discount"/>
                <field name="amount_total_discount"
                       widget="monetary"
                       options="{'currency_field': 'currency_id'}"
                       groups="account_invoice_fixed_discount.group_fixed_discount"/>
            </xpath>
        </field>
    </record>
</odoo>